{
  "name": "openclaw-mission-control",
  "version": "0.1.0",
  "private": true,
  "description": "Local Mission Control app and HTTP API for OpenClaw agents.",
  "scripts": {
    "start": "node dev-server.js",
    "check": "node --check dev-server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
